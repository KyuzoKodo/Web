console.log(arr[j])

               /* console.log("vergleiche: " + filter_out[i] + "und " + arr[j] )
                if(filter_out[i]!=arr[j]){
                 filterd_arr.push(filter_out)
                console.log(filter_out[i] + "ungleich " + arr[j] ) } */
    // Die Schleife wird anzahl der zu filterenden Zahlen wiederholt.
    // Also 2 
    // Während der itteration hole ich mir den Wert und vergleiche ihn mit allen Werten im vorhanden Array
    // Wenn der zufilternede Wert nicht mit dem Wert im aktuellen Array übereinstimmt dann wollen wir diesen Wert der nicht übereinstimmt in ein neues Array packen
    }
{ 
  
  
  /*
  for(let j=0;i<=arr.length;j++){

    console.log("vergleiche: " + filter_out[i] + "und " + arr[j] )
    if(filter_out[i]!=arr[j]){
      filterd_arr.push(filter_out)
      console.log(filter_out[i] + "ungleich " + arr[j] )
    }
    // Die Schleife wird anzahl der zu filterenden Zahlen wiederholt.
    // Also 2 
    // Während der itteration hole ich mir den Wert und vergleiche ihn mit allen Werten im vorhanden Array
    // Wenn der zufilternede Wert nicht mit dem Wert im aktuellen Array übereinstimmt dann wollen wir diesen Wert der nicht übereinstimmt in ein neues Array packen
  }
  */